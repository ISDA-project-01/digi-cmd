[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "digishell"
version = "0.1.0"
description = "AI Master Shell with Ollama qwen2.5:3b and instruction flags"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
digishell = "digishell.cli:main"
